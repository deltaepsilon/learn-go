package reverse_rotate_test

import (
	"testing"

	. "github.com/onsi/ginkgo"
	. "github.com/onsi/gomega"
)

func TestReverseRotate(t *testing.T) {
	RegisterFailHandler(Fail)
	RunSpecs(t, "ReverseRotate Suite")
}
